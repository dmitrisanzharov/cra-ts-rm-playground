import React from 'react';
import ContextBlock from '../context2';

// @ts-ignore 

type Props = any;

const Blah: React.FC<any> = (props: Props) => {

    return <div>
        <ContextBlock />
    </div>;
};

export default Blah;

